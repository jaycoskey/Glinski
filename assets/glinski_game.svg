<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="900" height="900" viewBox="-450 -450 900 900" >
  <!-- By Jay Coskey, 2026-Feb
    NOTE: This SVG displays a customizable view of an empty hexagonal chess board.
          It is customizable in a few ways:
       (1) The colors can be changed. See SPACE_COLORS.
       (2) Three types of labels can be enabled or disabled:
             * File labels @ bottom of board    (DO_ADD_FILE_LABELS)
             * Row labels at @ the sides & top  (DO_ADD_ROW_LABELS)
             * Space labels, inside the "hexes" (DO_ADD_SPACE_LABELS)
       (3) Other configuration values:
             * FONT_COLOR, FONT_SIZE, FONT_WEIGHT, etc.

    Higher priority:
    TODO: Add here or in an app: title above, footer below, Move history to the right.
    TODO: Add CSS.

    Lower priority:
    TODO: Low-pri: Add highlighting to indicate possible piece movement.
    TODO: Low-pri: Add arrows to indicate pending movement.
    TODO: Low-pri: Add animation.
  -->
  <defs>
    <!-- == Using Wikimedia SVG chess pieces (lt45 set) == -->
    <!-- King from Wikimedia Commons (Chess_klt45.svg) -->
    <symbol id="K"
      style="fill-rule:evenodd;
        stroke-linecap:round; stroke-linejoin:round">
      <path stroke-linejoin="miter" d="M22.5 11.63V6M20 8h5" stroke-width="2.5" />
      <path stroke-linecap="butt" stroke-linejoin="miter"
        d="M22.5 25s4.5-7.5 3-10.5c0 0-1-2.5-3-2.5s-3 2.5-3 2.5c-1.5 3 3
          10.5 3 10.5"/>
      <path
        d="M12.5 37c5.5 3.5 14.5 3.5 20 0v-7s9-4.5
          6-10.5c-4-6.5-13.5-3.5-16 4V27v-3.5c-2.5-7.5-12-10.5-16-4-3 6
          6 10.5 6 10.5v7"/>
      <path d="M12.5 30c5.5-3 14.5-3 20 0m-20 3.5c5.5-3 14.5-3 20 0m-20
          3.5c5.5-3 14.5-3 20 0"/>
    </symbol>

    <!-- Queen from Wikimedia Commons (Chess_qlt45.svg) -->
    <symbol id="Q"
      style="stroke-linejoin:round">
      <path d="M 9,26
        C 17.5,24.5 30,24.5 36,26
        L 38.5,13.5
        L 31,25
        L 30.7,10.9
        L 25.5,24.5
        L 22.5,10
        L 19.5,24.5
        L 14.3,10.9
        L 14,25
        L 6.5,13.5
        L 9,26 z"/>
      <path d="M 9,26
        C 9,28 10.5,28 11.5,30
        C 12.5,31.5 12.5,31 12,33.5
        C 10.5,34.5 11,36 11,36
        C 9.5,37.5 11,38.5 11,38.5
        C 17.5,39.5 27.5,39.5 34,38.5
        C 34,38.5 35.5,37.5 34,36
        C 34,36 34.5,34.5 33,33.5
        C 32.5,31 32.5,31.5 33.5,30
        C 34.5,28 36,28 36,26
        C 27.5,24.5 17.5,24.5 9,26 z"/>
      <path d="M 11.5,30 C 15,29 30,29 33.5,30" style="fill:none"/>
      <path d="M 12,33.5 C 18,32.5 27,32.5 33,33.5" style="fill:none"/>
      <circle cx="6"    cy="12" r="2" />
      <circle cx="14"   cy="9"  r="2" />
      <circle cx="22.5" cy="8"  r="2" />
      <circle cx="31"   cy="9"  r="2" />
      <circle cx="39"   cy="12" r="2" />
    </symbol>

    <!-- Rook from Wikimedia Commons (Chess_rlt45.svg) -->
    <symbol id="R"
      style="opacity:1; fill-opacity:1;
      fill-rule:evenodd; stroke-linecap:round; stroke-linejoin:round;
      stroke-miterlimit:4; stroke-dasharray:none; stroke-opacity:1;"
      transform="translate(0,0.3)">
      <path
        d="M 9,39 L 36,39 L 36,36 L 9,36 L 9,39 z "
        style="stroke-linecap:butt;" />
      <path
        d="M 12,36 L 12,32 L 33,32 L 33,36 L 12,36 z "
        style="stroke-linecap:butt;" />
      <path
        d="M 11,14
          L 11,9 L 15,9 L 15,11 L 20,11 L 20,9 L 25,9
          L 25,11 L 30,11 L 30,9 L 34,9 L 34,14"
        style="stroke-linecap:butt;" />
      <path
        d="M 34,14 L 31,17 L 14,17 L 11,14" />
      <path
        d="M 31,17 L 31,29.5 L 14,29.5 L 14,17"
        style="stroke-linecap:butt; stroke-linejoin:miter;" />
      <path
        d="M 31,29.5 L 32.5,32 L 12.5,32 L 14,29.5" />
      <path
        d="M 11,14 L 34,14"
        style="stroke-linejoin:miter;" />
    </symbol>

    <!-- Bishop from Wikimedia Commons (Chess_blt45.svg) -->
    <symbol id="B"
      style="opacity:1; fill-rule:evenodd; fill-opacity:1;
        stroke-linecap:round; stroke-linejoin:round;
        stroke-miterlimit:4; stroke-dasharray:none;
        stroke-opacity:1;"
      transform="translate(0,0.6)">
      <g style="stroke-linecap:butt;">
        <path d="M 9,36
          C 12.39,35.03 19.11,36.43 22.5,34
          C 25.89,36.43 32.61,35.03 36,36
          C 36,36 37.65,36.54 39,38
          C 38.32,38.97 37.35,38.99 36,38.5
          C 32.61,37.53 25.89,38.96 22.5,37.5
          C 19.11,38.96 12.39,37.53 9,38.5
          C 7.65,38.99 6.68,38.97 6,38
          C 7.35,36.54 9,36 9,36 z"/>
        <path d="M 15,32
          C 17.5,34.5 27.5,34.5 30,32
          C 30.5,30.5 30,30 30,30
          C 30,27.5 27.5,26 27.5,26
          C 33,24.5 33.5,14.5 22.5,10.5
          C 11.5,14.5 12,24.5 17.5,26
          C 17.5,26 15,27.5 15,30
          C 15,30 14.5,30.5 15,32 z"/>
        <path d="M 25 8
          A 2.5 2.5 0 1 1  20,8
          A 2.5 2.5 0 1 1  25 8 z" />
      </g>
      <path d="M 17.5,26
        L 27.5,26
        M 15,30
        L 30,30
        M 22.5,15.5
        L 22.5,20.5
        M 20,18
        L 25,18" stroke-width="2.5" />
    </symbol>

    <!-- Knight from Wikimedia Commons (Chess_nlt45.svg) -->
    <symbol id="N"
      style="opacity:1; fill-opacity:1; fill-rule:evenodd;
        stroke-linecap:round;stroke-linejoin:round;stroke-miterlimit:4;
        stroke-dasharray:none; stroke-opacity:1;"
      transform="translate(0,0.3)">
      <path
        d="M 22,10
          C 32.5,11 38.5,18 38,39
          L 15,39
          C 15,30 25,32.5 23,18" />
      <path
        d="M 24,18
          C 24.38,20.91 18.45,25.37 16,27
          C 13,29 13.18,31.34 11,31
          C 9.958,30.06 12.41,27.96 11,28
          C 10,28 11.19,29.23 10,30
          C 9,30 5.997,31 6,26
          C 6,24 12,14 12,14
          C 12,14 13.89,12.1 14,10.5
          C 13.27,9.506 13.5,8.5 13.5,7.5
          C 14.5,6.5 16.5,10 16.5,10
          L 18.5,10
          C 18.5,10 19.28,8.008 21,7
          C 22,7 22,10 22,10" />
      <path
        d="M 9.5 25.5
          A 0.5 0.5 0 1 1 8.5,25.5
          A 0.5 0.5 0 1 1 9.5 25.5 z" />
      <path
        d="M 15 15.5 A 0.5 1.5 0 1 1  14,15.5 A 0.5 1.5 0 1 1  15 15.5 z"
        transform="matrix(0.866,0.5,-0.5,0.866,9.693,-5.173)" />
    </symbol>

    <!-- Pawn from Wikimedia Commons (Chess_plt45.svg) -->
    <symbol id="P"
      style="opacity:1; fill-opacity:1; fill-rule:nonzero;
        stroke-linecap:round; stroke-linejoin:miter; stroke-miterlimit:4;
        stroke-dasharray:none; stroke-opacity:1;">
      <path d="m 22.5,9
        c -2.21,0 -4,1.79 -4,4 0,0.89 0.29,1.71 0.78,2.38
        C 17.33,16.5 16,18.59 16,21
        c 0,2.03 0.94,3.84 2.41,5.03
        C 15.41,27.09 11,31.58 11,39.5
        H 34
        C 34,31.58    29.59,27.09  26.59,26.03
          28.06,24.84 29,23.03     29,21
          29,18.59    27.67,16.5   25.72,15.38
          26.21,14.71 26.5,13.89   26.5,13
        c 0,-2.21 -1.79,-4 -4,-4 z" />
    </symbol>

    <script type="application/ecmascript"><![CDATA[
      // ========================================
      // Feature constants
      const DO_ADD_FILE_LABELS = true;
      const DO_ADD_ROW_LABELS = true;
      const DO_ADD_SPACE_LABELS = false;
      const DO_ADD_PIECES = true;

      const FILE_LABELS_TXT = "abcdefghikl".split("");
      // ========================================
      // Font constants
      const FONT_COLOR       = "black";      // e.g., "red", "black", "white", etc.
      const FONT_FAMILY      = "Comic Sans MS";  // "Times New Roman";
      const FONT_SIZE        = 20;
      const FONT_WEIGHT      = "bold";       // e.g., "normal", "bold", "400", "700", etc.

      const SPACE_FONT_COLOR  = FONT_COLOR;
      const SPACE_FONT_FAMILY = FONT_FAMILY;
      const SPACE_FONT_SIZE   = "16";         // FONT_SIZE;
      const SPACE_FONT_WEIGHT = FONT_WEIGHT;

      // Define font constants used by column and row labels
      const EXT_FONT_COLOR   = FONT_COLOR;
      const EXT_FONT_FAMILY  = FONT_FAMILY;
      const EXT_FONT_SIZE    = FONT_SIZE;
      const EXT_FONT_WEIGHT  = FONT_WEIGHT;

      // ========================================
      // SVG constants
      const SVG_NS = "http://www.w3.org/2000/svg";

      // ========================================
      // Layout constants
      const BOARD_SIDE_HEX_COUNT = 6;  // For Glinski's hexagonal chess boards

      // Note: Board units are spaces
      const BOARD_RADIUS = BOARD_SIDE_HEX_COUNT - 1;  // Units = hexes

      const HEX_WIDTH_HALF  = 40;  // Half-width (= dist from center to vertex)
      const HEX_HEIGHT_HALF = (Math.sqrt(3) / 2) * HEX_WIDTH_HALF;

      const HEX_WIDTH  = 2 * HEX_WIDTH_HALF;
      const HEX_HEIGHT = 2 * HEX_HEIGHT_HALF;

      // ========================================
      // Color constants
      const COLOR_BLACK = "#000000";
      const SPACE_BORDER_COLOR = COLOR_BLACK;
      const SPACE_BORDER_WIDTH = 2;  // Note: Borders lie within spaces, not between them.

      const LIGHT_YELLOW = "#ffff55";
      const MED_GREEN =    "#33ee33";
      const DARK_PURPLE =  "#a922a9";

      const DEFAULT_PALETTE = {
        Light:  "#ffff77",  // Yellow
        Medium: "#55ff55",  // Green
        Dark:   "#dd77ff"   // Light purple
      };

      const BLUE_TONES_PALETTE = {
        Light:  "#bfbfff",  // Light blue
        Medium: "#8f8fff",  // Medium blue
        Dark:   "#5f5fff"   // Dark blue
      };
      const BOLD_EARTH_PALETTE = {
        Light:  "#ffcf9f",  // Light tan
        Medium: "#d99d60",  // Medium tan
        Dark:   "#b26c25"   // Brown
      };
      const COLORFUL_PALETTE = {
        Light:  LIGHT_YELLOW,
        Medium: MED_GREEN,
        Dark:   DARK_PURPLE
      };
      const DULL_EARTH_PALETTE = {  // From Wikipedia art "Hexagonal chess"
        Light:  "#ffcf9f",  // Light tan
        Medium: "#e9ad70",  // Medium tan
        Dark:   "#d28c45"   // Dark tan
      };
      const GRAY_PALETTE = {
        Light:  "#cfcfcf",
        Medium: "#9f9f9f",
        Dark:   "#6f6f6f",
      };
      const YELLOW_BLUE_BOARD_PALETTE = {
        Light:  "#ffffbf",  // Yellow
        Medium: "#9f9f9f",  // Gray
        Dark:   "#2f5faf"   // Blue
      };

      // ========================================
      // Set color palette
      // To switch: Copy a palette from below, replacing the next line, then uncomment it.
      const SPACE_COLORS = BOLD_EARTH_PALETTE;

      // const SPACE_COLORS = BLUE_TONES_PALETTE;
      // const SPACE_COLORS = BOLD_EARTH_PALETTE;
      // const SPACE_COLORS = COLORFUL_PALETTE;
      // const SPACE_COLORS = DEFAULT_PALETTE;
      // const SPACE_COLORS = DULL_EARTH_PALETTE;
      // const SPACE_COLORS = GRAY_PALETTE;
      // const SPACE_COLORS = YELLOW_BLUE_BOARD_PALETTE;

      // ========================================
      A1=[-5,-5];A2=[-5,-4];A3=[-5,-3];A4=[-5,-2];A5=[-5,-1];A6=[-5,0]
      B1=[-4,-5];B2=[-4,-4];B3=[-4,-3];B4=[-4,-2];B5=[-4,-1];B6=[-4,0];B7=[-4,1]
      C1=[-3,-5];C2=[-3,-4];C3=[-3,-3];C4=[-3,-2];C5=[-3,-1];C6=[-3,0];C7=[-3,1];C8=[-3,2]
      D1=[-2,-5];D2=[-2,-4];D3=[-2,-3];D4=[-2,-2];D5=[-2,-1];D6=[-2,0];D7=[-2,1];D8=[-2,2];D9=[-2,3]
      E1=[-1,-5];E2=[-1,-4];E3=[-1,-3];E4=[-1,-2];E5=[-1,-1];E6=[-1,0];E7=[-1,1];E8=[-1,2];E9=[-1,3];E10=[-1,4]
      F1=[0,-5];F2=[0,-4];F3=[0,-3];F4=[0,-2];F5=[0,-1];F6=[0,0];F7=[0,1];F8=[0,2];F9=[0,3];F10=[0,4];F11=[0,5]
      G1=[1,-4];G2=[1,-3];G3=[1,-2];G4=[1,-1];G5=[1,0];G6=[1,1];G7=[1,2];G8=[1,3];G9=[1,4];G10=[1,5]
      H1=[2,-3];H2=[2,-2];H3=[2,-1];H4=[2,0];H5=[2,1];H6=[2,2];H7=[2,3];H8=[2,4];H9=[2,5]
      I1=[3,-2];I2=[3,-1];I3=[3,0];I4=[3,1];I5=[3,2];I6=[3,3];I7=[3,4];I8=[3,5]
      K1=[4,-1];K2=[4,0];K3=[4,1];K4=[4,2];K5=[4,3];K6=[4,4];K7=[4,5]
      L1=[5,0];L2=[5,1];L3=[5,2];L4=[5,3];L5=[5,4];L6=[5,5]

      // TODO: Add pieces
      PIECE_COORDS = {
        "black": {
          "K": [G10],
          "Q": [E10],
          "R": [C8, H8],
          "B": [F9, F10, F11],
          "N": [D9, H9],
          "P": [B7, C7, D7, E7, F7, G7, H7, I7, K7],
          },
        "white": {
          "K": [G1],
          "Q": [E1],
          "R": [C1, I1],
          "B": [F1, F2, F3],
          "N": [D1, H1],
          "P": [B1, C2, D3, E4, F5, G4, H3, I2, K1],
          }
        }

      // ========================================
      /** Convert space coords (a, b) to pixel coords (x,y) for flat-topped hexes
        In this implementation of HexVec (hexagonal vector) coords:
          - the first axis is oriented 30 degrees clockwise from Right/East.
          - the second axis is oriented Upward/Northward.
      */
      function spaceCoordsToPixelCoords(a, b) {
        const x = HEX_WIDTH_HALF * (3/2 * a);
        const y = HEX_HEIGHT * (a/2 - b);
        return { x, y };
      }

      /** Compute vertex coords for a flat-topped hex space centered at (cx, cy) */
      function hexVertices(cx, cy) {
        let pts = [];
        for (let i = 0; i < 6; i++) {
          const angle = Math.PI / 180 * (60 * i);  // in radians
          const x = cx + HEX_WIDTH_HALF * Math.cos(angle);
          const y = cy + HEX_WIDTH_HALF * Math.sin(angle);
          pts.push(x.toFixed(4) + "," + y.toFixed(4));
        }
        return pts.join(" ");
      }

      /** =========================
         Coloring rule constraints:
           - Center space has color Medium.
           - Directly above any Medium-colored space is a Light-colored space.
           - Directly below any Medium-colored space is a Dark-colored space.
           - Neighbors of a space, iterating clockwise, alternate between the other 2 colors.
         ========================= */
      function setExternalLabelAttributes(label) {
        label.setAttribute("fill", EXT_FONT_COLOR);
        label.setAttribute("font-family", EXT_FONT_FAMILY);
        label.setAttribute("font-size", EXT_FONT_SIZE.toString() + "px");
        label.setAttribute("font-weight", EXT_FONT_WEIGHT);
        label.setAttribute("text-anchor", "middle");
      }

      function setInternalLabelAttributes(label) {
        label.setAttribute("fill", SPACE_FONT_COLOR);
        label.setAttribute("font-family", SPACE_FONT_FAMILY);
        label.setAttribute("font-size", SPACE_FONT_SIZE.toString() + "px");
        label.setAttribute("font-weight", SPACE_FONT_WEIGHT);
        label.setAttribute("text-anchor", "middle");
      }

      function spaceColor(a, b) {
        let idx = (a - 2*b) % 3;
        if (idx < 0) idx += 3;

        if (idx === 0) { return SPACE_COLORS.Medium; }
        else if (idx === 1) { return SPACE_COLORS.Light; }
        else return SPACE_COLORS.Dark;
      }

      // ========================================
      function addBoard() {
        const board = document.getElementById("HexagonalChessBoard");

        for (let a = -BOARD_RADIUS; a <= BOARD_RADIUS; a++) {
          for (let b = -BOARD_RADIUS; b <= BOARD_RADIUS; b++) {
            const grid_dist = a - b;
            if (Math.abs(grid_dist) <= BOARD_RADIUS) {
              const { x, y } = spaceCoordsToPixelCoords(a, b);
              const hex = document.createElementNS(SVG_NS, "polygon");
              hex.setAttribute("points", hexVertices(x, y));
              hex.setAttribute("fill", spaceColor(a, b));
              hex.setAttribute("stroke", SPACE_BORDER_COLOR);
              hex.setAttribute("stroke-width", SPACE_BORDER_WIDTH);
              board.appendChild(hex);
            }
          }
        }
        if (DO_ADD_FILE_LABELS) {
          addLabelsFiles();
        }
        if (DO_ADD_ROW_LABELS) {
          addLabelsForRowsLeft();
          addLabelsForRowsRight();
        }
        if (DO_ADD_SPACE_LABELS) {
          addLabelsSpaces();
        }
        if (DO_ADD_PIECES) {
          addPieces(PIECE_COORDS);
        }
      }
      function addLabelsSpaces() {
        const board = document.getElementById("HexagonalChessBoard");

        for (let a = -BOARD_RADIUS; a <= BOARD_RADIUS; a++) {
          for (let b = -BOARD_RADIUS; b <= BOARD_RADIUS; b++) {
            const grid_dist = a - b;
            if (Math.abs(grid_dist) <= BOARD_RADIUS) {
              const { x, y } = spaceCoordsToPixelCoords(a, b);
              const label = document.createElementNS(SVG_NS, "text");
              label.setAttribute("x", x);
              label.setAttribute("y", y + EXT_FONT_SIZE/4);
              colTxt = FILE_LABELS_TXT[a + 5];
              rowTxt = (6 + b - (a >= 0 ? a : 0)).toString();
              label.textContent = colTxt + rowTxt;
              setInternalLabelAttributes(label);
              board.appendChild(label);
            }
          }
        }
      }

      function addLabelsFiles() {
        const board = document.getElementById("HexagonalChessBoard");
        const FILE_LABELS_POS = [
          [-5,-6], [-4, -6], [-3,-6], [-2, -6], [-1, -6],
          [0, -6],
          [1, -5], [2, -4], [3, -3], [4, -2], [5, -1]
          ];
        for (let k = 0; k < FILE_LABELS_TXT.length; k++) {
          const label = document.createElementNS(SVG_NS, "text");
          const [a, b] = FILE_LABELS_POS[k];
          const {x, y} = spaceCoordsToPixelCoords(a, b);
          label.setAttribute("x", x);
          label.setAttribute("y", y - EXT_FONT_SIZE/2);
          label.textContent = FILE_LABELS_TXT[k];
          setExternalLabelAttributes(label);
          board.appendChild(label);
        }
      }

      function addLabelsForRowsLeft() {
        const board = document.getElementById("HexagonalChessBoard");
        const ROW_LABELS_POS = [
          [-5.7, -5.05], [-5.7, -4.05], [-5.7, -3.05], [-5.7, -2.05], [-5.7, -1.05],
          [-5.7, -0.05],
          [-4.7, 0.9], [-3.7, 1.9], [-2.7, 2.9], [-1.73, 3.95], [-0.73, 4.95]
          ]
        const ROW_LABELS_TXT = Array.from(Array(11), (_, i)=> ((i+1).toString()));
        for (let k = 0; k < ROW_LABELS_TXT.length; k++) {
          const label = document.createElementNS(SVG_NS, "text");
          const [a, b] = ROW_LABELS_POS[k];
          const {x, y} = spaceCoordsToPixelCoords(a, b);
          label.setAttribute("x", x);
          label.setAttribute("y", y);
          label.textContent = ROW_LABELS_TXT[k];
          setExternalLabelAttributes(label);
          board.appendChild(label);
        }
      }

      function addLabelsForRowsRight() {
        const board = document.getElementById("HexagonalChessBoard");
        const ROW_LABELS_POS = [
          [5.7, 0.65], [5.7, 1.65], [5.7, 2.65], [5.7, 3.65], [5.7, 4.65],
          [5.7, 5.65],
          [4.7, 5.6], [3.7, 5.6], [2.7, 5.6], [1.75, 5.7], [0.75, 5.7],
          ]
        const ROW_LABELS_TXT = Array.from(Array(11), (_, i)=> ((i+1).toString()));
        for (let k = 0; k < ROW_LABELS_TXT.length; k++) {
          const label = document.createElementNS(SVG_NS, "text");
          const [a, b] = ROW_LABELS_POS[k];
          const {x, y} = spaceCoordsToPixelCoords(a, b);
          label.setAttribute("x", x);
          label.setAttribute("y", y);
          label.textContent = ROW_LABELS_TXT[k];
          setExternalLabelAttributes(label);
          board.appendChild(label);
        }
      }

      function addPieceAt(pieceId, style, a, b) {
        const {x, y} = spaceCoordsToPixelCoords(a, b);
        const board = document.getElementById("HexagonalChessBoard");
        const piece = document.createElementNS(SVG_NS,"use");
        piece.setAttribute("transform", `translate(${x-28},${y-30}) scale(1.25)`);
        piece.setAttribute("href", "#" + pieceId);
        piece.setAttribute("fill", style.piece_fill);
        piece.setAttribute("stroke", style.piece_outline_color);
        piece.setAttribute("stroke-width", style.piece_outline_width);
        piece.setAttribute("stroke-linejoin", "round");
        board.appendChild(piece);
      }

      PIECE_STYLES = {
        "black": { piece_fill: "black", piece_outline_color: "white", piece_outline_width: "0.75" },
        "white": { piece_fill: "white", piece_outline_color: "black", piece_outline_width: "1.0" }
        }

      function addPieces(pieceCoords) {
        for (const color in pieceCoords) {
            for (const id in pieceCoords[color]) {
                for (const coords of pieceCoords[color][id]) {
                    addPieceAt(id, PIECE_STYLES[color], coords[0], coords[1]);
                }
            }
        }
      }

      document.addEventListener("DOMContentLoaded", addBoard);
    ]]></script>
  </defs>

  <!-- Here is the actual object representing the board -->
  <g id="HexagonalChessBoard" />
</svg>
